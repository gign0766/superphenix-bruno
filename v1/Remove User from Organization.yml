info:
  name: Remove User from Organization
  type: http
  seq: 15
  tags:
    - v1
    - iam

http:
  method: DELETE
  url: "{{baseUrl}}/v1/organization/:orgaId/iam/invite"
  headers:
    - name: Authorization
      value: "{{apiKey}}"
      description: Access token used to check user access in Superphenix API.
  params:
    - name: orgaId
      value: ""
      type: path
      description: Organization ID
    - name: userId
      value: ""
      type: query
      description: User to Remove
  auth:
    type: apikey
    key: Authorization
    value: "{{apiKey}}"
    placement: header

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/v1/organization/:orgaId/iam/invite"
      method: DELETE
      headers:
        - name: Authorization
          value: "{{apiKey}}"
          description: Access token used to check user access in Superphenix API.
      params:
        - name: orgaId
          value: ""
          type: path
          description: Organization ID
        - name: userId
          value: ""
          type: query
          description: User to Remove
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
  - name: 400 Response
    description: Bad Request
    request:
      url: "{{baseUrl}}/v1/organization/:orgaId/iam/invite"
      method: DELETE
      headers:
        - name: Authorization
          value: "{{apiKey}}"
          description: Access token used to check user access in Superphenix API.
      params:
        - name: orgaId
          value: ""
          type: path
          description: Organization ID
        - name: userId
          value: ""
          type: query
          description: User to Remove
    response:
      status: 400
      statusText: Bad Request
      body:
        type: text
        data: ""
  - name: 403 Response
    description: Forbidden
    request:
      url: "{{baseUrl}}/v1/organization/:orgaId/iam/invite"
      method: DELETE
      headers:
        - name: Authorization
          value: "{{apiKey}}"
          description: Access token used to check user access in Superphenix API.
      params:
        - name: orgaId
          value: ""
          type: path
          description: Organization ID
        - name: userId
          value: ""
          type: query
          description: User to Remove
    response:
      status: 403
      statusText: Forbidden
      body:
        type: text
        data: ""
  - name: 500 Response
    description: Internal Server Error
    request:
      url: "{{baseUrl}}/v1/organization/:orgaId/iam/invite"
      method: DELETE
      headers:
        - name: Authorization
          value: "{{apiKey}}"
          description: Access token used to check user access in Superphenix API.
      params:
        - name: orgaId
          value: ""
          type: path
          description: Organization ID
        - name: userId
          value: ""
          type: query
          description: User to Remove
    response:
      status: 500
      statusText: Internal Server Error
      body:
        type: text
        data: ""

docs: Remove a user from an organization

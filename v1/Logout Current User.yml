info:
  name: Logout Current User
  type: http
  seq: 2
  tags:
    - v1
    - session

http:
  method: GET
  url: "{{baseUrl}}/v1/logout"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 302 Response
    description: Found
    request:
      url: "{{baseUrl}}/v1/logout"
      method: GET
    response:
      status: 302
      statusText: Found
      body:
        type: text
        data: ""

docs: Invalidate the refresh token and redirect to default url

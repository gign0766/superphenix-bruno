info:
  name: Retrieve all security groups
  type: http
  seq: 121
  tags:
    - v1
    - Superphenix Controller

http:
  method: GET
  url: "{{baseUrl}}/:orgaId/api/spx-ctrl/:projectId/security-group"
  headers:
    - name: Authorization
      value: "{{apiKey}}"
      description: Access token used to check user access in Superphenix API.
  params:
    - name: orgaId
      value: ""
      type: path
      description: Organization ID
    - name: projectId
      value: ""
      type: path
      description: Project ID
  auth:
    type: apikey
    key: Authorization
    value: "{{apiKey}}"
    placement: header

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: SecurityGroups
    request:
      url: "{{baseUrl}}/:orgaId/api/spx-ctrl/:projectId/security-group"
      method: GET
      headers:
        - name: Authorization
          value: "{{apiKey}}"
          description: Access token used to check user access in Superphenix API.
      params:
        - name: orgaId
          value: ""
          type: path
          description: Organization ID
        - name: projectId
          value: ""
          type: path
          description: Project ID
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            {
              "codeAZ": "",
              "eid": "",
              "gitops": "",
              "id": "",
              "productName": "",
              "productTypeId": "",
              "securityGroup": ""
            }
          ]
  - name: 500 Response
    description: Internal Server Error
    request:
      url: "{{baseUrl}}/:orgaId/api/spx-ctrl/:projectId/security-group"
      method: GET
      headers:
        - name: Authorization
          value: "{{apiKey}}"
          description: Access token used to check user access in Superphenix API.
      params:
        - name: orgaId
          value: ""
          type: path
          description: Organization ID
        - name: projectId
          value: ""
          type: path
          description: Project ID
    response:
      status: 500
      statusText: Internal Server Error
      body:
        type: text
        data: ""

docs: Retrieve all security groups across AZ

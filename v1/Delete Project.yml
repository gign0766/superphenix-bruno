info:
  name: Delete Project
  type: http
  seq: 19
  tags:
    - v1
    - project

http:
  method: DELETE
  url: "{{baseUrl}}/v1/organization/:orgaId/project"
  headers:
    - name: Authorization
      value: "{{apiKey}}"
      description: Access token used to check user access in Superphenix API.
  params:
    - name: orgaId
      value: ""
      type: path
      description: Organization ID
    - name: projectId
      value: ""
      type: query
      description: Project ID
  auth:
    type: apikey
    key: Authorization
    value: "{{apiKey}}"
    placement: header

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/v1/organization/:orgaId/project"
      method: DELETE
      headers:
        - name: Authorization
          value: "{{apiKey}}"
          description: Access token used to check user access in Superphenix API.
      params:
        - name: orgaId
          value: ""
          type: path
          description: Organization ID
        - name: projectId
          value: ""
          type: query
          description: Project ID
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
  - name: 400 Response
    description: Bad Request
    request:
      url: "{{baseUrl}}/v1/organization/:orgaId/project"
      method: DELETE
      headers:
        - name: Authorization
          value: "{{apiKey}}"
          description: Access token used to check user access in Superphenix API.
      params:
        - name: orgaId
          value: ""
          type: path
          description: Organization ID
        - name: projectId
          value: ""
          type: query
          description: Project ID
    response:
      status: 400
      statusText: Bad Request
      body:
        type: text
        data: ""
  - name: 500 Response
    description: Internal Server Error
    request:
      url: "{{baseUrl}}/v1/organization/:orgaId/project"
      method: DELETE
      headers:
        - name: Authorization
          value: "{{apiKey}}"
          description: Access token used to check user access in Superphenix API.
      params:
        - name: orgaId
          value: ""
          type: path
          description: Organization ID
        - name: projectId
          value: ""
          type: query
          description: Project ID
    response:
      status: 500
      statusText: Internal Server Error
      body:
        type: text
        data: ""

docs: Delete Project
